import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ArrowRight, Target, Users, TrendingUp, Shield } from "lucide-react";
import Link from "next/link";

const AboutUs = () => {
  return (
    <section className="py-20 lg:py-28 bg-slate-50">
      <div className="container mx-auto px-6">
        <div className="text-center mb-16">
          <h2 className="text-3xl md:text-4xl font-bold text-foreground mb-6">
            Qui sommes-nous ?
          </h2>
          <p className="text-xl text-muted-foreground max-w-3xl mx-auto">
            Pilot Immo s'adresse à deux profils complémentaires pour créer un écosystème 
            gagnant-gagnant dans l'immobilier complexe.
          </p>
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 max-w-6xl mx-auto">
          {/* Investisseurs */}
          <Card id="investisseurs" className="bg-card shadow-lg hover:shadow-2xl transition-all duration-300 border-2 border-primary/20 rounded-2xl transform hover:-translate-y-2">
            <CardContent className="p-8">
              <div className="text-center mb-6">
                <div className="w-20 h-20 bg-primary rounded-full flex items-center justify-center mx-auto mb-4">
                  <TrendingUp className="w-10 h-10 text-primary-foreground" />
                </div>
                <h3 className="text-2xl font-bold text-foreground mb-2">
                  Vous êtes investisseur
                </h3>
                <p className="text-muted-foreground">
                  Vous recherchez des opportunités d'investissement à fort potentiel
                </p>
              </div>

              <div className="space-y-4 mb-8 text-left">
                <FeatureItem icon={<Target className="text-primary" />} title="Biens sélectionnés" description="Accédez à notre portefeuille de biens à fort potentiel de plus-value" />
                <FeatureItem icon={<Shield className="text-primary" />} title="Accompagnement complet" description="De l'acquisition à la valorisation, nous gérons toute la chaîne" />
                <FeatureItem icon={<TrendingUp className="text-primary" />} title="Rentabilité optimisée" description="Stratégies d'investissement adaptées à vos objectifs" />
              </div>

              <Button asChild variant="default" className="w-full group" size="lg">
                <Link href="#contact">
                  <span>Découvrir nos opportunités</span>
                  <ArrowRight className="w-5 h-5 ml-2 group-hover:translate-x-1 transition-transform" />
                </Link>
              </Button>
            </CardContent>
          </Card>

          {/* Propriétaires */}
          <Card id="proprietaires" className="bg-card shadow-lg hover:shadow-2xl transition-all duration-300 border-2 border-accent/20 rounded-2xl transform hover:-translate-y-2">
            <CardContent className="p-8">
              <div className="text-center mb-6">
                <div className="w-20 h-20 bg-accent rounded-full flex items-center justify-center mx-auto mb-4">
                  <Users className="w-10 h-10 text-accent-foreground" />
                </div>
                <h3 className="text-2xl font-bold text-foreground mb-2">
                  Vous êtes propriétaire
                </h3>
                <p className="text-muted-foreground">
                  Votre bien pose problème et vous cherchez des solutions
                </p>
              </div>

              <div className="space-y-4 mb-8 text-left">
                <FeatureItem icon={<Shield className="text-accent" />} title="Solutions sur-mesure" description="Diagnostic et plan d'action adaptés à votre situation" />
                <FeatureItem icon={<Target className="text-accent" />} title="Gestion déléguée" description="Nous prenons en charge tous les aspects problématiques" />
                <FeatureItem icon={<Users className="text-accent" />} title="Rachat possible" description="Option de rachat de votre bien si c'est la meilleure solution" />
              </div>

              <Button asChild variant="outline" className="w-full group border-accent text-accent hover:bg-accent hover:text-accent-foreground" size="lg">
                 <Link href="#contact">
                  <span>Demander un diagnostic</span>
                  <ArrowRight className="w-5 h-5 ml-2 group-hover:translate-x-1 transition-transform" />
                </Link>
              </Button>
            </CardContent>
          </Card>
        </div>
      </div>
    </section>
  );
};

const FeatureItem = ({ icon, title, description }: { icon: React.ReactNode, title: string, description: string }) => (
  <div className="flex items-start space-x-3">
    <div className="w-5 h-5 mt-1 flex-shrink-0">{icon}</div>
    <div>
      <p className="font-medium text-foreground">{title}</p>
      <p className="text-sm text-muted-foreground">{description}</p>
    </div>
  </div>
);

export default AboutUs;
